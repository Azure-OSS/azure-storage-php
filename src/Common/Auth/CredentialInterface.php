<?php

declare(strict_types=1);

namespace AzureOss\Storage\Common\Auth;

/**
 * Common interface for Azure Storage credentials
 */
interface CredentialInterface
{
    // Marker interface to allow both SharedKey and Workload Identity credentials
}